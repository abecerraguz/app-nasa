<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container">
    {{#unless nombre}}
      <a class="navbar-brand" href="/">
      <img  width="40" src="img/bicycle.png" alt="BikeShop" class="mx-2">
      BikeShop
      </a>
    {{/unless}}
    {{#if nombre}}
      <img  width="40" src="img/bicycle.png" alt="BikeShop" class="mx-2">
      BikeShop
    {{/if}}
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav ms-md-auto">
            {{#unless nombre}}
              <li class="nav-item">
                <a class="nav-link api" href="/">Búsqueda de ordenes</a>
              </li>
              <li class="nav-item">
                <a class="nav-link api" href="/APIv1">APIv1</a>
              </li>
            {{/unless}}

            {{#if logout}}
              
              <li class="nav-item user">
                  <img width="23" src="img/user.svg" alt="{{> partials/title}}">
                  {{> partials/title}}
              </li>
              <li class="nav-item closeSesion">
                <a class="nav-link" id="closeSesion" href="/">
                  <img width="23" src="img/sign-out.svg" alt="cerrar sesión">
                  Cerrar sesión
                </a>
              </li>
            {{/if}}

            {{#unless nombre}}
              {{!-- Inicio de dropdwon --}}
              <li class="dropdown">
                <a class="nav-link dropdown-toggle" href="#" id="dropdown04" data-bs-toggle="dropdown" aria-expanded="false">Login</a>
                <ul class="dropdown-menu" aria-labelledby="dropdown04" id="login-dp">
                    <li>
                        <div class="row">
                               <form autocomplete="off" id="formulario">
                                {{!-- <h2 class="mt-2 text-center">👽 Bienvenido a LA NASA 👽</h2> --}}
                                {{!-- <hr> --}}
                                {{!-- <p class="mt-3">{{> partials/title}}</p> --}}
                                <div class="mb-3">
                                    <input type="email" id="email" class="form-control" placeholder="Ingrese Email">
                                </div>
                                <div class="mb-3">
                                    <input type="password" id="password" class="form-control" placeholder="Ingrese Password">
                                </div>
                                <div class="mb-3">
                                    <button id="ingresar" class="btn btn-success m-auto text-uppercase">Ingresar</button>
                                </div>
                              </form>
                        </div>
                    </li>
                </ul>
              </li>
            {{/unless}}
            {{!-- Cierre de dropdwon --}}

              
              
            </ul>
    </div>
  </div>
</nav>


